return {
  "folke/flash.nvim",
  opts = {
    modes = {
    -- disable flash for
    -- `f`, `F`, `t`, `T`, `;` and `,` motions
      char = {
        enabled = false
        -- autohide = true
      }
    }
  },
}
